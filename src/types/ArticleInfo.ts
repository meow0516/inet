export type ArticleInfo = {
    author: string,
    body: string,
    coverImage: string,
    createdAt: string,
    id: number,
    lastModified: number,
    likes: number,
    title: string,
    views: number
  }